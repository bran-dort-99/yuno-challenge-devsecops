# macOS
.DS_Store
logs_62494772540/

# Claude Code local settings
.claude/

# Terraform state and local files
*.tfstate
*.tfstate.backup
.terraform/
# NOTE: .terraform.lock.hcl is intentionally committed — it pins provider
# versions and hashes for reproducible CI builds and supply chain security.
*.tfvars
*.tfvars.json
